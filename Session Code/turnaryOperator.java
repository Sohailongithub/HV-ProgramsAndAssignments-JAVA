public class turnaryOperator {
    public static void main(String[] args) {

        // int numOne = 10;
        // int numTwo = 20;
        // int numThree = 30;

        // int greatestNum = (numOne < numTwo) && (numTwo < numThree) ? numThree : numTwo; numTwo : numOne ; 


        


    }
}

// if ((a > b) && (a > c)) {
//             greatestNum = a;
//         }else if((b>a) && (b>c)){
//              greatestNum = b;
//         }else{
//              greatestNum = c;
//         }

// int greatest = (num1 > num2) ? ((num1 > num3) ? num1 : num3) : ((num2 > num3) ? num2 : num3);
